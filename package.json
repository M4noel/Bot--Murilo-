{
  "name": "telegram-bot-portfolio",
  "version": "1.0.0",
  "description": "Bot automático do Telegram com Vue.js e Vercel",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
